{% extends 'base.html.twig' %}

{% block title %}Edit Pin{% endblock %}

{% block body %}
    <div class="auth-card">
        <h1>Edit "{{ pin.title }}"</h1>
        {{ include('pin/_form.html.twig') }}
    </div>
{% endblock %}